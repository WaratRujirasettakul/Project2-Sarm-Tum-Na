using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Code_isattacking : MonoBehaviour
{
    public bool isattacking;
    // Start is called before the first frame update
    void Start()
    {
        isattacking = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
